npm init, nodemon, express.
set the file structure.
setting up the server backend.


the flow as now - the brow-base ai mediapipe/face-api is running and tracking the web cam.
when the model detect any suspicious act, it reaction will be send a mail, send an pre record call audio.
the data send should be{ "type": "Intrusion", "timestamp": "12:15 PM", "metadata": { "confidence": 0.92 } }.
for now the alret will get send to terminal, goal - to integrate the frontend with the backend.


now i have completed the integation of a simple simulation of call now i will work on the mail, the end goal for round 1 i am guessing to add the camera part if the frontend team get the page ready.